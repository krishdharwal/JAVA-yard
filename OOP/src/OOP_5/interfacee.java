package OOP_5;
// INTERFACES :-> these are an collection of different abstract methods
// interfaces are an type of class that can be implemented by an subclass
// and subclass can implement so many interfaces in one file

public interface interfacee {
    // all the variables in interfaces must be static and final
    static final int age = 222;

    String name(String name);
    void room(int room);
}
